# One-line edit campaign r1

Status: **data-gated; no main model trained or exported**. The contract, public-data pilots, evaluation controls, and training worker were prepared and tested. They do not constitute a quality result. The running automatic-feedback editor, its model, SQLite collector, and SigNoz services were left in place.

## Frozen scope and resources

The student initializer is the untouched `Qwen/Qwen2.5-Coder-0.5B` revision `8123ea2e9354afb7ffcc6c8641d1b2f5ecf18301`. The existing local weight SHA-256 is `aff8914ec707fcaf9e2d4dc97197cded50b1c63e1d3a7a82e56f54d83ea47f80`; tokenizer SHA-256 is `c0382117ea329cdf097041132f6d735924b697924d6f6fc3945713e96ce87539`. No model weights were downloaded. [Plan](one_line_r1/plan.json), [schema](one_line_r1/schema.md), and [plan revisions](one_line_r1/plan-revisions.md) freeze the one-row N/D/R/I action protocol, actual EOS, 64-token action cap, 1,024-token default input budget, grouping rules, and campaign limits. Suite revision 3 fixed EOF deletion before any accepted labels or model outputs.

Authenticated Kaggle quota at the final 2026-09-26 11:43:22 UTC refresh was 45.00 GPU-hours remaining of 45.00, renewing 2026-10-03 00:00 UTC, with no active jobs. Campaign limits were 24 T4x2 wall-hours, 100 million nonpadding training input tokens, and 12 GiB new persistent local research storage. Actual use: **0 GPU-hours, 0 training tokens, 0 teacher calls**. Candidate artifacts occupy 17,109,084 bytes locally. The [quota ledger](one_line_r1/quota_ledger.json) retains the authenticated source and job statuses.

## Data result and decision

Two public Git pilots each contain 100 candidate states from the same four licensed repositories across Python, TypeScript, Rust, and Go. All candidates pass byte-exact suite revision 3 application checks. The first has no history. The second replays prior edits from pinned parent blobs, but 95 edit targets use reconstructed file order and five keep controls are synthetic terminal states. Neither pilot has passed an independent inferability review. **Accepted distinct training, development, and test states are 0 / 0 / 0.** Mechanism categories are unreviewed, and there are four candidate source groups against the 100-group main-run floor. [Data audit](one_line_r1/data_audit.json), [source inventory](one_line_r1/source_inventory.json), and [failure audit](one_line_r1/failure_audit/data_gate.md) retain the reasons and candidate hashes.

The independent read-only audit of Continue Instinct's native TypeScript train split found 2,222 of 4,371 rows with a structurally one-line gold edit at the visible cursor and a TypeScript path. This is an upper bound before source, history, license, token, and duplicate checks. It does not satisfy the 20,000-state floor or the repository-group requirement. Its native test split was scanned for structure but not used for selection. Large Git-commit shards were inspected through remote metadata and were not downloaded.

The named OpenCode Go model was listed by the authenticated CLI, but no inference was sent. The current [OpenCode Terms of Use](https://opencode.ai/legal/terms-of-service) prohibit automated Output extraction and Output use to develop competing AI models. The proposed student-label route therefore stopped before calibration. [Teacher calibration status](one_line_r1/teacher_calibration.json) records zero calls and no substitute paid provider.

The main data gate requires 20,000 accepted train states, at least 100 source groups, and 25 reviewed mechanisms. The actual accepted count is zero. The `--execute` entry point stops before GPU allocation at this gate. It would be misleading to consume Kaggle time teaching the new action header on these candidates and call it the requested broad edit model.

The controller does not yet package or submit a Kaggle notebook after the data gate. The separate resumable GPU worker is implemented, but its q25 CUDA and optimizer path has not run. This is an unfinished execution step in addition to the data blocker; a future campaign must verify a Kaggle job bundle before using the worker for a main run.

## Implementation and evidence

The [contract](../../src/tinycomplete/one_line/contract.py) applies one physical-line keep, replacement, deletion, or insertion byte-exactly across LF, CRLF, EOF, tabs, and Unicode. The [context serializer](../../src/tinycomplete/one_line/context.py) keeps answer-bearing split and mechanism metadata out of prompts. The [evaluation scorer](../../src/tinycomplete/one_line/evaluate.py) separates valid wire output, EOS, edit-required success, keep false edits, and unknown alternative edits. It has independent gold/unchanged controls, development-only display calibration, grouped uncertainty, paired outcomes, and a one-shot final-test claim. These are implementation tests, not model scores.

The [training primitives](../../src/tinycomplete/one_line/train.py) supervise response and real EOS positions only, average target loss within each example and then across examples, and select the causal logits needed for target positions. They enforce token and deadline caps and save full restart state with hashes. A tiny Qwen2 CPU test compared selected-logit gradients and continuous versus save/reload updates; this does not validate q25 CUDA throughput or bitsandbytes. The Kaggle [worker](../../scripts/train_one_line.py) validates the exact pretrained files, dataset manifest, split, source license, action reconstruction, planned tokens, and storage before model load. Its CPU inspection of 64 disposable synthetic rows found 9,570 nonpadding input tokens and 128 supervised target-plus-EOS tokens over two planned updates. No update was run on those rows. The [campaign entry point](../../scripts/run_one_line_campaign.py) checks the frozen plan, live quota, one-job rule, token cap, and cumulative campaign artifact storage.

No LR pilot, learning curve, sealed final evaluation, F16 export, Q4 conversion, or laptop sanity measurement occurred. Consequently there is no new model-quality, output-length, latency, or memory comparison. The older four-recipe adapter and live Q4 deployment remain historical controls only.

Full repository verification using the existing CPU Torch environment and the worktree's dependencies passed: **308 tests passed, 1 skipped**. The one-line tests passed **69/69** in that environment. `uv run ruff check .` passed, and `uv run mypy src scripts/run_one_line_campaign.py scripts/train_one_line.py` found no errors in 71 source files. `uv build --wheel` succeeded and the wheel includes the new one-line package. The fresh worktree's plain `uv run pytest -q` could not collect five older test modules because Torch is not declared in that environment; the combined existing Torch environment supplied it for the complete run. The explicit campaign `--execute` check returned `data gate pending: fewer than 20,000 distinct accepted training states` before any GPU action.

The [architecture memo](one_line_r1/architecture_memo.md) finds that the exact Mamba-3 443M checkpoint's published source requires BF16 and TMA paths that are not a validated T4 FP16 training path; no supported target-CPU Q4 inference path or exact-checkpoint code-edit evidence was found. No Mamba weights were downloaded. This evidence does not justify replacing q25.

## Handoff

The recoverable state is [HANDOFF.md](one_line_r1/HANDOFF.md). A later main run needs a lawful teacher-output route or a new public/authorized edit source with genuinely inferable next actions, reviewed source licenses, group-disjoint splits, and at least 20,000 accepted states. It must revise the frozen plan when the data or suite changes, then run the two locked LR pilots before any main SFT. Automatic editor promotion remains outside this campaign.
