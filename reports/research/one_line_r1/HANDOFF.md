# One-line r1 handoff

Branch: `research/one-line-r1`, based on `dda0218f0e92e99668026d1d44928506af2bc24f`. The source worktree is `/home/crabcake/Projects/tabcomplete-one-line-r1`. Do not move the live automatic-feedback worktree or its systemd service paths.

The plan is frozen at suite revision 3 and `single-line-context-v2`. Its current plan SHA-256 is `856ccdfe6aa6473931cbc4d77f928c9bbfa12a373525b4167ef5039bc852d286`. Earlier plan revisions remain for audit. The approved untouched q25 snapshot is already local; verify its hashes through `uv run python scripts/run_one_line_campaign.py --config configs/research/one_line_r1.yaml`.

`--execute` currently stops at the data gate: zero accepted training states versus the 20,000 minimum. Do not turn the 200 exploratory Git rows into accepted labels by changing their metadata alone. The history-conditioned pilot has 100/100 structural replay success, but only four repositories and no independent inferability or objective-check acceptance. The Continue Instinct structural upper bound is 2,222 native TypeScript training rows at the visible cursor, before provenance filters. The OpenCode teacher route is blocked by the current output-use terms; no call was made.

The next justified data step is a small source-grounded pilot that makes the intended edit independently checkable from information visible to the student. Audit source licenses and repository identity first. If its acceptance yield is real, register a new plan/data revision, keep group-disjoint test data sealed, then expand in the declared 100 -> 1,000 stages. Do not allocate a GPU until at least 20,000 distinct accepted train states and the other source/mechanism gates pass.

The training worker accepts local pinned model/data paths and a locked LR selection. Run its CPU inspect mode first, then submit it through the existing Kaggle orchestrator only after the campaign runner's live quota and storage checks pass. Its complete checkpoint must be validated by save/process-exit/reload before main SFT. No session or checkpoint exists in r1.

The running desktop editor, collector DB `/mnt/ssd/collector-data/collector.sqlite`, SigNoz, and the existing adapted q25 Q4 artifact were untouched. This branch contains no replacement inference weight. The TabComplete sealed test was not scored.
